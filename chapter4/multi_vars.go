package main

import "fmt"

// Method of defining multiple variables where they do not require specific type setting. Can be constants too.

var (
	a = "Let's"
	b = "define"
	c = "multiple"
	d = "variables!"
)

func main() {
	fmt.Println(a, b, c, d)
}
